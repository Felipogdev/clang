#include <stdio.h>
#include <math.h>

int main (){
    float a = sqrt(9);
    float b = pow(2, 4); // seria 2 elevado a 4
    int c = round(3.14); // arredondar 
    int d = ceil(3.14); // arredondar para cima
    int e = floor(3.14); // arredondar para baixo
    float f = fabs(-100); //converte numeros negativos pra positivos
    float g = log(3);
    float h = sin(45);
    float i = cos(45);
    float j = tan(45);
}